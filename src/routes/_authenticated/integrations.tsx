import { createFileRoute } from "@tanstack/react-router";
import { Bell, Check, Github, Mail, MessageSquare, Save } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Switch } from "@/components/ui/switch";
import { Kicker, Panel, SectionHeader } from "@/components/app-shell";
export const Route = createFileRoute("/_authenticated/integrations")({ component: IntegrationsPage });
function IntegrationsPage() { const [email,setEmail]=useState("tpsreehari.2000@gmail.com"); const [teamUrl,setTeamUrl]=useState(""); const [digest,setDigest]=useState(true); const [saved,setSaved]=useState(false); return <div><SectionHeader eyebrow="INTEGRATIONS / SIGNALS" title="Connect the outside loop" description="Secrets stay out of source control. Add them here when you are ready; the workspace is useful without any of these connections." /><div className="grid gap-5 lg:grid-cols-2"><Panel><div className="flex items-center gap-3"><Github className="h-5 w-5 text-primary" /><div><Kicker>Source control</Kicker><h2 className="mt-1 text-lg font-semibold">GitHub activity</h2></div><span className="ml-auto rounded-full border border-emerald-400/30 px-2 py-1 text-[10px] text-emerald-400">CONNECTED</span></div><p className="mt-4 text-sm leading-6 text-muted-foreground">Pushes, branches, and pull requests can attach to tasks when a commit message includes a task ref such as FMV-14.</p><div className="mt-5 grid gap-3 sm:grid-cols-[1fr_auto]"><Input defaultValue="thunder-1007/project-command-center" /><Button variant="outline">Sync now</Button></div></Panel><Panel><div className="flex items-center gap-3"><MessageSquare className="h-5 w-5 text-primary" /><div><Kicker>Team channel</Kicker><h2 className="mt-1 text-lg font-semibold">Microsoft Teams</h2></div></div><p className="mt-4 text-sm leading-6 text-muted-foreground">Post blockers, task moves, and daily digest updates to your channel using an incoming webhook.</p><div className="mt-5 flex gap-3"><Input placeholder="https://... incoming webhook" value={teamUrl} onChange={e=>setTeamUrl(e.target.value)} /><Button variant="outline" onClick={()=>setSaved(true)}>{saved ? <Check className="h-4 w-4"/> : "Save"}</Button></div></Panel><Panel><div className="flex items-center gap-3"><Mail className="h-5 w-5 text-primary" /><div><Kicker>Reminder delivery</Kicker><h2 className="mt-1 text-lg font-semibold">Email via Resend</h2></div></div><p className="mt-4 text-sm leading-6 text-muted-foreground">Daily digest and per-task due reminders will be sent to the project inbox. Add the API key and verified sender in the environment later.</p><div className="mt-5 space-y-3"><Input value={email} onChange={e=>setEmail(e.target.value)} /><div className="flex items-center justify-between rounded border border-border p-3 text-sm"><span className="flex items-center gap-2"><Bell className="h-4 w-4 text-primary"/>Daily digest at 09:00</span><Switch checked={digest} onCheckedChange={setDigest}/></div></div></Panel><Panel><Kicker>Future channel</Kicker><h2 className="mt-1 text-lg font-semibold">SMS / WhatsApp</h2><p className="mt-4 text-sm leading-6 text-muted-foreground">The reminder engine keeps delivery pluggable. Add a provider when the project needs mobile alerts; no task model rebuild required.</p><Button className="mt-5" variant="outline" disabled><Save className="mr-2 h-4 w-4"/> Provider not configured</Button></Panel></div></div>; }
