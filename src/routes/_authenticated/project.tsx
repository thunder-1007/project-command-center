import { createFileRoute } from "@tanstack/react-router";
import { CheckCircle2, Circle, Layers3, LockKeyhole, Workflow } from "lucide-react";
import { Kicker, Panel, SectionHeader } from "@/components/app-shell";
export const Route = createFileRoute("/_authenticated/project")({ component: ProjectPage });
const phases = ["Discovery & product framing", "CaseFlow workflow design", "Form builder and data model", "Rules, approvals and audit trail", "Employee onboarding pilot", "Measure adoption and iterate"];
function ProjectPage() { return <div><SectionHeader eyebrow="PROJECT / CASEFLOW" title="Formiva Mission Control" description="The living brief for the private CaseFlow build. This is the source of truth for scope, architecture decisions, and the sensitive-pilot gate." /><div className="grid gap-5 xl:grid-cols-[1.4fr_1fr]"><Panel><div className="flex items-center gap-3"><Layers3 className="h-5 w-5 text-primary" /><div><Kicker>North star</Kicker><h2 className="mt-1 text-lg font-semibold">Make employee onboarding auditable, fast, and calm.</h2></div></div><p className="mt-5 max-w-2xl text-sm leading-7 text-muted-foreground">Formiva turns the onboarding workflow into a structured CaseFlow: a reusable intake, the right approvals, clear ownership, and an evidence trail that can stand up to sensitive-pilot scrutiny.</p><div className="mt-6 grid gap-3 sm:grid-cols-3">{[["Scope","Private Formiva project"],["Launch motion","Employee onboarding"],["Primary signal","Cycle time + auditability"]].map(([a,b]) => <div key={a} className="rounded-md border border-border bg-background/50 p-3"><Kicker>{a}</Kicker><div className="mt-2 text-sm">{b}</div></div>)}</div></Panel><Panel><div className="flex items-center gap-3"><LockKeyhole className="h-5 w-5 text-primary" /><div><Kicker>Architecture layers</Kicker><h2 className="mt-1 text-lg font-semibold">Deliberately boring foundations</h2></div></div><div className="mt-5 space-y-3">{["Capture — forms and structured intake", "Orchestrate — rules, stages, approvals", "Evidence — audit log and exports", "Measure — time-to-complete and adoption"].map((item,i) => <div key={item} className="flex items-center gap-3 border-b border-border/60 pb-3 text-sm last:border-0"><span className="mono text-xs text-primary">0{i+1}</span>{item}</div>)}</div></Panel></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><Panel><div className="flex items-center justify-between"><div><Kicker>Build sequence</Kicker><h2 className="mt-1 text-lg font-semibold">Roadmap checkpoints</h2></div><Workflow className="h-5 w-5 text-primary" /></div><div className="mt-5 space-y-3">{phases.map((phase,i) => <div className="flex items-center gap-3" key={phase}>{i < 2 ? <CheckCircle2 className="h-4 w-4 text-emerald-400" /> : <Circle className="h-4 w-4 text-muted-foreground" />}<span className={i < 2 ? "text-sm" : "text-sm text-muted-foreground"}>{phase}</span><span className="mono ml-auto text-[10px] text-muted-foreground">P{i}</span></div>)}</div></Panel><Panel><Kicker>Sensitive pilot gate</Kicker><h2 className="mt-1 text-lg font-semibold">Before real employee data</h2><div className="mt-5 space-y-3">{["Owner and role boundaries tested", "Audit events visible for every state change", "Data retention and export path agreed", "Pilot cohort and success metrics named"].map((item,i) => <div className="flex items-start gap-3 text-sm" key={item}><div className="mt-0.5 h-4 w-4 rounded border border-primary/50" /><span>{item}</span><span className="mono ml-auto text-[10px] text-muted-foreground">0{i+1}</span></div>)}</div></Panel></div></div>; }
